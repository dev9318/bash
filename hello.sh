#! /bin/bash

# echo $0 $1 # $0 prints name of the script and $1 the first argument it is called with 

# args=("$@") # stores the arguments as an array

# echo ${args[0]} # doesn't include the script name
# echo $@ # prints all the agruments except script name

# echo $# #prints the number of arguments passed

# echo "Hello"
# echo "Enter two names:"
# read name name1
# read -a names (this is to read an array)
# names[0] et al can be used bsically any array operation
# read -sp "enter password:" pass
# echo "Name: $name and $name1"
# echo "$pass"
